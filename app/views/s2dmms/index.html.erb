<p>スクレイピングNo.2</p>
<h2>こちらは入力したワードの画像をネット上から収集して表示するシステムです。</h2>
<p>著作権のライセンスフリーの画像のみを集めています！</p>

<% if @img_srcs.present? %>
	<% @img_srcs.each do |i| %>
		<img src="<%= i.image_url %>">
	<% end %>
<% end %>

<h1><%= link_to"レモンの画像を集める", s2dmms_path, method: :post %></h1>
<p>※検索完了まで10〜30秒ほどかかることがあります</p>
<p>※たまに検索に失敗します。失敗するとバグります。その場合はページを更新してください。</p>

<%= link_to"トップへ戻る",root_path%>

